mixin button(text)
    .row
        .col-12.d-flex.align-items-center.flex-column.justify-content-center
            .button(class!=attributes.class)
                button.button__btn(id!=attributes.id)
                    img(src!=attributes.src).button__img
                    =text